== A Boilerplate rails app to build on for projects.

This app already has:

* A Home page

* Custom font and styling

* Header and footer partials


This app will soon have:

* Users integration with Devise

* Use PG as the database

* Font-Awesome integration